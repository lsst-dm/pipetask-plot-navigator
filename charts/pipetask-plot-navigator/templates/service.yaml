---
apiVersion: v1
kind: Service
metadata:
  name: pipe-analysis-navigator
spec:
  selector:
    app: pipe-analysis-navigator
  ports:
  - port: 80
    protocol: TCP
    targetPort: {{ .Values.port | default 8080 }}
